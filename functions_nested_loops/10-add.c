#include "main.h"
/**
* add - Entry point
* @a: input number
* @b : input number
* Return: Always one resultado
*/
int add(int a, int b)
{
	int resultado;

	resultado = a + b;

	return (resultado);
}
