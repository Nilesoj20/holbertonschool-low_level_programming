#include <stdio.h>
/**
* mul - Entry point
* @a: First operand
* @b: Second operand
* Return: Always 0 (Success)
*/
int mul(int a, int b)
{
	int multiplica;

	multiplica = a * b;
	return (multiplica);
}
