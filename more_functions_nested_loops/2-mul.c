#include <stdio.h>
/**
* mul - Entry point
* @a: First operand
* @b: Second operand
* @mul: First member
* Return: Always 0 (Success)
*/
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
